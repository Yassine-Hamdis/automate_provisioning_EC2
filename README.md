main branch :
  creating the infrastructure for AWS server, then deploy to ec2 default components
  
modules branch :
  creating our infrastructure modules (subnet + webserver ) then launch an EC2 server

eks branch :
  using pvc module, eks module to create an EKS-cluster
